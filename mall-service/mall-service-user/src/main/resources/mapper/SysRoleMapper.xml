<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cc.oxshan.user.mapper.SysRoleMapper">

    <resultMap id="BaseResultMap" type="cc.oxshan.user.entity.SysRole">
        <id column="id" property="id"/>
        <result column="shop_id" property="shopId"/>
        <result column="template_id" property="templateId"/>
        <result column="code" property="code"/>
        <result column="name" property="name"/>
        <result column="type" property="type"/>
        <result column="is_system" property="isSystem"/>
        <result column="status" property="status"/>
        <result column="sort" property="sort"/>
        <result column="remark" property="remark"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="selectRolesByUserId" resultMap="BaseResultMap">
        SELECT r.*
        FROM sys_role r
        INNER JOIN sys_user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
          AND r.status = 1
    </select>

    <select id="selectPage" resultMap="BaseResultMap">
        SELECT *
        FROM sys_role
        WHERE shop_id = #{shopId}
        ORDER BY sort ASC, created_at DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countByShopId" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM sys_role
        WHERE shop_id = #{shopId}
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT *
        FROM sys_role
        WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_role (shop_id, template_id, code, name, type, is_system, status, sort, remark)
        VALUES (#{shopId}, #{templateId}, #{code}, #{name}, #{type}, #{isSystem}, #{status}, #{sort}, #{remark})
    </insert>

    <update id="updateById">
        UPDATE sys_role
        SET name = #{name},
            status = #{status},
            sort = #{sort},
            remark = #{remark}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM sys_role
        WHERE id = #{id}
    </delete>

    <select id="countUsersByRoleId" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM sys_user_role
        WHERE role_id = #{roleId}
    </select>

    <delete id="deleteRoleMenus">
        DELETE FROM sys_role_menu
        WHERE role_id = #{roleId}
    </delete>

    <insert id="insertRoleMenus">
        INSERT INTO sys_role_menu (role_id, menu_id)
        VALUES
        <foreach collection="menuIds" item="menuId" separator=",">
            (#{roleId}, #{menuId})
        </foreach>
    </insert>

    <select id="selectMenuIdsByRoleId" resultType="java.lang.Long">
        SELECT menu_id
        FROM sys_role_menu
        WHERE role_id = #{roleId}
    </select>

</mapper>
