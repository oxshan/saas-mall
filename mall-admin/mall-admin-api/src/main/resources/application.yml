server:
  port: 8080

spring:
  application:
    name: mall-admin-api

dubbo:
  application:
    name: mall-admin-api
  registry:
    address: nacos://localhost:8848
  consumer:
    check: false
    timeout: 30000

jwt:
  secret: your-256-bit-secret-key-here-must-be-long-enough-for-hmac-sha256
  expiration: 7200000
